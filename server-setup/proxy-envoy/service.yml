apiVersion: v1
kind: Service
metadata:
  name: envoy-proxy
  namespace: yahyeapps
spec:
  type: ClusterIP
  selector:
    app: envoy
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
